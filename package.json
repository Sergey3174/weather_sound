{
  "name": "weather_sound",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://sergey3174.github.io/weather_sound",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "6.2.0",
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  }
}
